---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { t, type Locale, defaultLocale } from '@/i18n';

const locale = (Astro.currentLocale ?? defaultLocale) as Locale;
const i18n = t(locale);

const GITHUB_URL = 'https://github.com/inumansoul/kotauth';

const columns = [
  {
    status: 'Disponível',
    description: 'No ar hoje na v1.19.2.',
    legendDot: 'bg-[#1F9D55]',
    badgeWrap: 'bg-[#EAF7EF] border border-[#CDEBD9]',
    badgeDot: 'bg-[#1F9D55]',
    badgeText: 'text-[#1F8A4C]',
    cardClass: 'bg-white border border-kotauth-surface-2 shadow-[0_1px_3px_rgba(15,22,41,0.04)]',
    titleClass: 'text-kotauth-text-primary',
    bodyClass: 'text-kotauth-text-secondary',
    items: [
      { title: 'Núcleo OAuth 2.0 e OIDC', body: 'Provedor completo com PKCE, rotação de tokens e discovery.' },
      { title: 'Workspaces multi-tenant', body: 'Diretórios, chaves e políticas isolados por tenant.' },
      { title: 'Servidor MCP nativo de IA', body: '33 ferramentas com escopo em 8 domínios.' },
      { title: 'Telas de auth white-label', body: 'Renderizadas no servidor, com tema por workspace.' },
      { title: 'Magic-link e Email OTP', body: 'Fluxos sem senha que permanecem compatíveis com MFA.' },
      { title: 'Backup e restauração criptografados', body: 'Snapshots de tenant portáteis via CLI ou API.' },
    ],
  },
  {
    status: 'Em andamento',
    description: 'Em desenvolvimento ativo para as próximas versões.',
    legendDot: 'bg-kotauth-primary',
    badgeWrap: 'bg-[#EAF6FB] border border-[#CDE8F5]',
    badgeDot: 'bg-kotauth-primary',
    badgeText: 'text-kotauth-primary',
    cardClass: 'bg-white border border-kotauth-surface-2 border-l-[3px] border-l-kotauth-primary shadow-[0_1px_3px_rgba(15,22,41,0.04)]',
    titleClass: 'text-kotauth-text-primary',
    bodyClass: 'text-kotauth-text-secondary',
    items: [
      { title: 'SSO empresarial', body: 'Federação LDAP e SAML para provedores de identidade upstream.' },
      { title: 'Passkeys e WebAuthn', body: 'Login resistente a phishing ao lado dos métodos existentes.' },
      { title: 'Streaming de eventos via webhook', body: 'Inscreva sistemas downstream em eventos de auditoria e ciclo de vida.' },
      { title: 'Personificação de admin', body: 'Sessões de suporte com escopo e totalmente auditadas.' },
    ],
  },
  {
    status: 'Planejado',
    description: 'Comprometido, escopo definido e na fila.',
    legendDot: 'bg-[#D9890E]',
    badgeWrap: 'bg-[#FBF1E2] border border-[#F0DCBC]',
    badgeDot: 'bg-[#D9890E]',
    badgeText: 'text-[#B5730C]',
    cardClass: 'bg-white border border-kotauth-surface-2 shadow-[0_1px_3px_rgba(15,22,41,0.04)]',
    titleClass: 'text-kotauth-text-primary',
    bodyClass: 'text-kotauth-text-secondary',
    items: [
      { title: 'Provisionamento SCIM', body: 'Sincronização automatizada do ciclo de vida de usuários com diretórios externos.' },
      { title: 'Autorização granular', body: 'Controle de acesso baseado em relacionamentos para permissões complexas.' },
      { title: 'Gestão de organização self-service', body: 'Permita que usuários finais criem e administrem suas próprias organizações.' },
      { title: 'Exportação de log de auditoria', body: 'Envie para destinos SIEM e armazenamento de objetos de forma agendada.' },
    ],
  },
  {
    status: 'Explorando',
    description: 'Em pesquisa. Avise se você precisa disso.',
    legendDot: 'bg-[#A6AEBA]',
    badgeWrap: 'bg-[#F2F3F5] border border-kotauth-surface-2',
    badgeDot: 'bg-[#A6AEBA]',
    badgeText: 'text-[#7A8694]',
    cardClass: 'bg-[#FBFBFC] border border-dashed border-[#D8DCE3]',
    titleClass: 'text-kotauth-text-secondary',
    bodyClass: 'text-kotauth-text-muted',
    items: [
      { title: 'Device authorization flow', body: 'Faça login em dispositivos com entrada limitada, como TVs e CLIs.' },
      { title: 'Autenticação step-up', body: 'Exija fatores mais fortes para operações sensíveis.' },
      { title: 'Policy-as-code', body: 'Políticas de segurança versionadas e aplicadas no momento do deploy.' },
      { title: 'Hooks de cobrança por organização', body: 'Sinais de uso para equipes que constroem produtos sobre o Kotauth.' },
    ],
  },
];
---

<BaseLayout
  title={i18n.pages.roadmap.title}
  description={i18n.pages.roadmap.description}
  path="/roadmap"
>
  <!-- Page header -->
  <section class="relative px-7 pt-32 pb-14 text-center overflow-hidden">
    <div
      class="absolute -top-[120px] left-1/2 -translate-x-1/2 w-[900px] h-[420px] pointer-events-none"
      style="background:radial-gradient(ellipse at center,rgba(0,119,168,.1),transparent 70%);"
    ></div>
    <div class="relative max-w-[720px] mx-auto">
      <span class="eyebrow">Roadmap</span>
      <h1 class="font-heading font-bold text-4xl sm:text-5xl lg:text-[54px] leading-[1.06] tracking-[-0.03em] text-kotauth-text-primary mt-[14px] mb-[18px]">
        O que vamos construir a seguir
      </h1>
      <p class="text-lg leading-[1.6] text-kotauth-text-secondary font-medium">
        O Kotauth é de código aberto e construído de forma aberta. Aqui está onde as coisas estão e para onde elas vão. As prioridades mudam com a participação da comunidade.
      </p>
    </div>
  </section>

  <!-- Status legend -->
  <section class="max-w-[920px] mx-auto px-7 pb-10">
    <div class="flex flex-wrap justify-center gap-x-[22px] gap-y-3 text-[13px] font-semibold text-kotauth-text-secondary">
      {columns.map((col) => (
        <span class="inline-flex items-center gap-[7px]">
          <span class={`w-[9px] h-[9px] inline-block ${col.legendDot}`}></span>
          {col.status}
        </span>
      ))}
    </div>
  </section>

  <!-- Timeline -->
  <section class="max-w-[920px] mx-auto px-7 pb-20">
    {columns.map((col, i) => (
      <div
        class={`grid grid-cols-1 md:grid-cols-[200px_1fr] gap-8 py-[34px] border-t border-kotauth-surface-2 ${i === columns.length - 1 ? 'border-b' : ''}`}
      >
        <div>
          <div class={`inline-flex items-center gap-2 px-[11px] py-[5px] mb-2 ${col.badgeWrap}`}>
            <span class={`w-2 h-2 inline-block ${col.badgeDot}`}></span>
            <span class={`text-[12.5px] font-bold ${col.badgeText}`}>{col.status}</span>
          </div>
          <p class="text-[13px] text-kotauth-text-muted leading-[1.5]">{col.description}</p>
        </div>
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-[14px]">
          {col.items.map((item) => (
            <div class={`px-5 py-[18px] ${col.cardClass}`}>
              <h3 class={`font-heading text-[15px] font-bold mb-1 ${col.titleClass}`}>{item.title}</h3>
              <p class={`text-[13px] leading-[1.5] ${col.bodyClass}`}>{item.body}</p>
            </div>
          ))}
        </div>
      </div>
    ))}
  </section>

  <!-- Shape it -->
  <section class="max-w-[920px] mx-auto px-7 pb-28">
    <div class="bg-kotauth-bg-secondary border border-kotauth-surface-2 px-10 py-11 text-center">
      <h2 class="font-heading font-bold text-[28px] tracking-[-0.02em] text-kotauth-text-primary mb-[10px]">Tenha voz no que vem a seguir</h2>
      <p class="text-base leading-[1.6] text-kotauth-text-secondary font-medium max-w-[480px] mx-auto mb-6">
        As prioridades do roadmap são definidas em público. Abra uma discussão, registre um pedido ou vote no que importa para você.
      </p>
      <div class="flex flex-wrap justify-center gap-[13px]">
        <a
          href={`${GITHUB_URL}/discussions`}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-2 bg-kotauth-primary hover:bg-kotauth-primary-light text-white px-[22px] h-12 text-[14.5px] font-semibold transition-colors"
        >
          Entrar na discussão
        </a>
        <a
          href={`${GITHUB_URL}/issues/new`}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-2 bg-white border border-kotauth-surface-2 text-kotauth-text-primary hover:border-kotauth-primary/40 px-[22px] h-12 text-[14.5px] font-semibold transition-colors"
        >
          Solicitar um recurso
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
